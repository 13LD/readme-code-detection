RSpec.describe Readme::Code::Detection do
  it "has a version number" do
    expect(Readme::Code::Detection::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
